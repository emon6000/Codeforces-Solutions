/** * author:  EmonTheBOT * created: 27.02.2026 20:24:42 **/ #include <bits/stdc++.h>typedef long long ll;using namespace std;const long long MOD = 1000000007; #define fast                     \    ios::sync_with_stdio(false); \    cin.tie(nullptr)#define yes cout << "YES\n"#define no cout << "NO\n"#define one cout << 1 << "\n"#define zero cout << 0 << "\n" ll gcdll(ll a, ll b) { return b ? gcdll(b, a % b) : a; }ll lcmll(ll a, ll b) { return (a / gcdll(a, b)) * b; } ll modAdd(ll a, ll b) { return ((a % MOD) + (b % MOD) + MOD) % MOD; }ll modSub(ll a, ll b) { return ((a % MOD) - (b % MOD) + MOD) % MOD; }ll modMul(ll a, ll b) { return ((a % MOD) * (b % MOD)) % MOD; } ll modPow(ll a, ll b){    ll res = 1;    a %= MOD;    if (a == 0)        return 0;    while (b > 0)    {        if (b & 1)            res = (res * a) % MOD;        a = (a * a) % MOD;        b >>= 1;    }    return res;} ll modDiv(ll a, ll b) { return modMul(a, modPow(b, MOD - 2)); } vector<ll> sievePrimes(ll n){    vector<bool> isPrime(n + 1, true);    isPrime[0] = isPrime[1] = false;    for (ll i = 2; i * i <= n; i++)    {        if (isPrime[i])        {            for (ll j = i * i; j <= n; j += i)                isPrime[j] = false;        }    }    vector<ll> primes;    for (ll i = 2; i <= n; i++)        if (isPrime[i])            primes.push_back(i);    return primes;} int main(){    fast;    // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);    ll t;    cin >> t;    while (t--)    {        ll n;        cin >> n;        ll x, y;        cin >> x >> y;        vector<ll> v(n);        for (ll i = 0; i < n; i++)        {            cin >> v[i];        }        if (is_sorted(v.begin(), v.end()))        {            for (ll i = 0; i < n; i++)            {                cout << v[i] << " ";            }            cout << endl;        }        else        {            vector<ll> b;            for (ll i = x; i < y; i++)            {                b.push_back(v[i]);            }            auto mn = min_element(b.begin(), b.end());            rotate(b.begin(), mn, b.end());            vector<ll> vp;            for (ll i = 0; i < x; i++)            {                vp.push_back(v[i]);            }            for (ll i = y; i < n; i++)            {                vp.push_back(v[i]);            }            ll k = vp.size();            for (ll i = 0; i < vp.size(); i++)            {                if (vp[i] > b[0])                {                    k = i;                    break;                }            }            for (ll i = 0; i < k; i++)            {                cout << vp[i] << " ";            }            for (ll i = 0; i < b.size(); i++)            {                cout << b[i] << " ";            }            for (ll i = k; i < vp.size(); i++)            {                cout << vp[i] << " ";            }            cout << endl;        }    }    return 0;}