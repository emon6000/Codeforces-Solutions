/** * author:  EmonTheBOT * created: 05.11.2025 01:54:54 **/ #include <bits/stdc++.h>typedef long long ll;using namespace std;const long long MOD = 1000000007; #define fast                     \    ios::sync_with_stdio(false); \    cin.tie(nullptr)#define yes cout << "YES\n"#define no cout << "NO\n"#define one cout << 1 << "\n"#define zero cout << 0 << "\n" ll gcdll(ll a, ll b) { return b ? gcdll(b, a % b) : a; }ll lcmll(ll a, ll b) { return (a / gcdll(a, b)) * b; } ll modAdd(ll a, ll b) { return ((a % MOD) + (b % MOD) + MOD) % MOD; }ll modSub(ll a, ll b) { return ((a % MOD) - (b % MOD) + MOD) % MOD; }ll modMul(ll a, ll b) { return ((a % MOD) * (b % MOD)) % MOD; } ll modPow(ll a, ll b){    ll res = 1;    a %= MOD;    if (a == 0)        return 0;    while (b > 0)    {        if (b & 1)            res = (res * a) % MOD;        a = (a * a) % MOD;        b >>= 1;    }    return res;} ll modDiv(ll a, ll b) { return modMul(a, modPow(b, MOD - 2)); } vector<ll> sievePrimes(ll n){    vector<bool> isPrime(n + 1, true);    isPrime[0] = isPrime[1] = false;    for (ll i = 2; i * i <= n; i++)    {        if (isPrime[i])        {            for (ll j = i * i; j <= n; j += i)                isPrime[j] = false;        }    }    vector<ll> primes;    for (ll i = 2; i <= n; i++)        if (isPrime[i])            primes.push_back(i);    return primes;} int main(){    fast;    // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);    ll t;    cin >> t;    while (t--)    {        ll n;        cin >> n;        vector<ll> v(n + 1);        for (ll i = 1; i <= n; i++) {            cin >> v[i];        }         ll mx = 0;        for (ll i = 1; i <= n; i++) {            mx = max(mx, v[i]);            if (i % 2 == 0) {                v[i] = mx;             }        }         ll ans = 0;        for (ll i = 1; i <= n; i += 2) {             ll mn = 2e18;                         if (i > 1) {                 mn = min(mn, v[i - 1]);            }            if (i < n) {                mn = min(mn, v[i + 1]);            }             if (mn == 2e18) {                continue;            }            ans += max(0LL, v[i] - mn + 1);        }         cout << ans << "\n";    }    return 0;}