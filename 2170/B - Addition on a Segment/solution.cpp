/** * author:  EmonTheBOT * created: 21.12.2025 14:33:14 **/ #include <bits/stdc++.h>typedef long long ll;using namespace std;const long long MOD = 1000000007; #define fast                     \    ios::sync_with_stdio(false); \    cin.tie(nullptr)#define yes cout << "YES\n"#define no cout << "NO\n"#define one cout << 1 << "\n"#define zero cout << 0 << "\n" ll gcdll(ll a, ll b) { return b ? gcdll(b, a % b) : a; }ll lcmll(ll a, ll b) { return (a / gcdll(a, b)) * b; } ll modAdd(ll a, ll b) { return ((a % MOD) + (b % MOD) + MOD) % MOD; }ll modSub(ll a, ll b) { return ((a % MOD) - (b % MOD) + MOD) % MOD; }ll modMul(ll a, ll b) { return ((a % MOD) * (b % MOD)) % MOD; } ll modPow(ll a, ll b){    ll res = 1;    a %= MOD;    if (a == 0)        return 0;    while (b > 0)    {        if (b & 1)            res = (res * a) % MOD;        a = (a * a) % MOD;        b >>= 1;    }    return res;} ll modDiv(ll a, ll b) { return modMul(a, modPow(b, MOD - 2)); } vector<ll> sievePrimes(ll n){    vector<bool> isPrime(n + 1, true);    isPrime[0] = isPrime[1] = false;    for (ll i = 2; i * i <= n; i++)    {        if (isPrime[i])        {            for (ll j = i * i; j <= n; j += i)                isPrime[j] = false;        }    }    vector<ll> primes;    for (ll i = 2; i <= n; i++)        if (isPrime[i])            primes.push_back(i);    return primes;} int main(){    fast;    // freopen("input.txt", "r", stdin);    // freopen("output.txt", "w", stdout);    ll t;    cin >> t;    while (t--)    {        ll n;        cin >> n;        vector<ll> v(n);        set<ll> st;        ll sum = 0;        for (ll i = 0; i < n; i++)        {            cin >> v[i];            sum += v[i];            if (v[i] != 0)                st.insert(v[i]);        }        sort(v.begin(), v.end());        ll o = 0, x = 0, mx = 0;        for (ll i = 0; i < n; i++)        {            if (v[i] == 1)            {                o++;                x++;                mx = max(mx, v[i]);            }            else if (v[i] > 1)            {                x++;                mx = max(mx, v[i]);            }        }        if (x == 0)        {            cout << 0 << endl;        }        else if (st.size() == x && x == n)        {            cout << n << endl;        }        else if (o == n || sum == n)        {            cout << 1 << endl;        }        else        {            ll rem = sum - x;            ll r = n - 1 - rem;            ll nn = n - 1;            if (rem >= nn)            {                cout << x << endl;            }            else            {                cout << x - r << endl;            }        }    }    return 0;}